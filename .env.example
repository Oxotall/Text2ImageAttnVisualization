# Copy this file to `.env` and fill in your token (the real `.env` is gitignored).
# Needed only for gated models (SDXL, SD 2.1); SD 1.5 works without it.
# Get a token at https://huggingface.co/settings/tokens
HF_TOKEN=
