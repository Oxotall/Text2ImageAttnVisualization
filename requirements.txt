# Install torch and torchvision together, the same way (all-pip), so their
# builds match — a torch/torchvision version mismatch causes import errors.
torch>=2.0.0
torchvision>=0.15.0
diffusers>=0.27.0
transformers>=4.38.0
accelerate>=0.27.0
safetensors>=0.4.0
numpy>=1.24.0
Pillow>=9.5.0
matplotlib>=3.7.0
# Notebook / interactive visualization
jupyter>=1.0.0
ipywidgets>=8.0.0
# Web app
flask>=3.0.0
